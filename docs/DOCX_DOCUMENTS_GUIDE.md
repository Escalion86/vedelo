# DOCX: документы в Ведело

## Что умеет система

- Формирует документы в `.docx` из пользовательских шаблонов.
- Подставляет переменные из карточки мероприятия, клиента и настроек реквизитов артиста.
- Поддерживает:
  - несколько пользовательских DOCX-шаблонов;
  - названия шаблонов;
  - типы документов: `Договор`, `Счет`, `Чек`, `Акт`, `Другое`;
  - стандартные DOCX-шаблоны договора и акта как примеры/fallback.

## Где включается в интерфейсе

Серверный endpoint формирования `/api/mobile/v1/events/[id]/documents/generate` принимает `documentDate` в формате `YYYY-MM-DD`. Несуществующий день (например, `2026-02-31`) возвращает HTTP 400 с кодом `DATE_INVALID`, без формирования и загрузки документа. Если дата не задана, используется текущая дата сервера.

1. Откройте `Документы`.
2. Нажмите `Добавить шаблон`.
3. Задайте название шаблона, выберите тип документа и загрузите `.docx`.
4. В редакторе мероприятия откройте `Финансы и Документы`.
5. В блоке `Сформировать документ` выберите нужный шаблон и скачайте Word-файл.
6. После формирования прикрепите файл или ссылку в блоке `Документы мероприятия`.

## Ограничение по тарифу

- Функционал доступен только если тариф разрешает `Работа с документами` (`allowDocuments = true`).
- Если доступа нет:
  - на странице `Документы` показывается уведомление о недоступности;
  - в редакторе мероприятия скрыт блок `Сформировать документ`;
  - скрыт блок `Документы мероприятия`.
- Чекбокс `По договору` в мероприятии виден всегда.

## Где хранятся шаблоны

Пользовательские шаблоны сохраняются в `SiteSettings.custom.documentTemplates[]`:

- `id`
- `name`
- `type`
- `customTypeName`
- `fileName`
- `templateBase64`
- `createdAt`
- `updatedAt`

Legacy-поля поддерживаются только на переходный период:

- `contractDocxTemplateBase64`
- `contractDocxTemplateFileName`
- `actDocxTemplateBase64`
- `actDocxTemplateFileName`

Стандартные шаблоны (fallback) берутся из:

- `public/templates/default-contract-template.docx`
- `public/templates/default-act-template.docx`

## Формат переменных в DOCX

Рекомендуемый формат тегов в шаблоне: `{ПЕРЕМЕННАЯ_С_ПОДЧЕРКИВАНИЯМИ}`.

Примеры:

- `{НОМЕР_ДОКУМЕНТА}`
- `{ДАТА_ДОГОВОРА}`
- `{НАИМЕНОВАНИЕ_КЛИЕНТА}`
- `{ДОГОВОРНАЯ_СУММА}`

## Переменные договора

- `{НОМЕР_ДОКУМЕНТА}`
- `{ОСНОВНОЙ_ГОРОД}`
- `{ДАТА_ДОГОВОРА}`
- `{НАИМЕНОВАНИЕ_КЛИЕНТА}`
- `{ФИО_КЛИЕНТА}`
- `{ФИО_АРТИСТА}`
- `{СТАТУС_АРТИСТА}`
- `{ОСНОВАНИЕ_АРТИСТА}`
- `{РЕКВИЗИТЫ_АРТИСТА}`
- `{ОГРНИП_АРТИСТА}`
- `{ИНН_АРТИСТА}`
- `{НАИМЕНОВАНИЕ_АРТИСТА}`
- `{ИСПОЛНИТЕЛЬ_ПОДПИСАНТ}`
- `{ДАТА}`
- `{СПИСОК_УСЛУГ}`
- `{ДОГОВОРНАЯ_СУММА}`
- `{ДАТА_СОБЫТИЯ}`
- `{ГОРОД_СОБЫТИЯ}`
- `{АДРЕС_СОБЫТИЯ}`
- `{ИСПОЛНИТЕЛЬ_ФИО}`
- `{КОНТАКТЫ_КЛИЕНТА}`
- `{РЕКВИЗИТЫ_КЛИЕНТА}`
- `{ИНН}`
- `{КПП}`
- `{ОГРН}`
- `{БАНК}`
- `{БИК}`
- `{РАСЧЕТНЫЙ_СЧЕТ}`
- `{КОРР_СЧЕТ}`
- `{ЮР_АДРЕС}`
- `{РЕКВИЗИТЫ_СТОРОН}`
- `{ПОДПИСИ_СТОРОН}`
- `{БАНК_АРТИСТА}`
- `{БИК_АРТИСТА}`
- `{РАСЧЕТНЫЙ_СЧЕТ_АРТИСТА}`
- `{КОРР_СЧЕТ_АРТИСТА}`
- `{ЮР_АДРЕС_АРТИСТА}`

## Переменные акта

- `{НОМЕР_ДОКУМЕНТА}`
- `{ДАТА_АКТА}`
- `{ДАТА_ДОГОВОРА}`
- `{ОСНОВНОЙ_ГОРОД}`
- `{НАИМЕНОВАНИЕ_КЛИЕНТА}`
- `{ФИО_КЛИЕНТА}`
- `{ФИО_АРТИСТА}`
- `{ИСПОЛНИТЕЛЬ_ПОДПИСАНТ}`
- `{НАИМЕНОВАНИЕ_АРТИСТА}`
- `{СПИСОК_УСЛУГ}`
- `{ДОГОВОРНАЯ_СУММА}`
- `{ДАТА_СОБЫТИЯ}`
- `{ГОРОД_СОБЫТИЯ}`
- `{АДРЕС_СОБЫТИЯ}`
- `{РЕКВИЗИТЫ_СТОРОН}`
- `{ПОДПИСИ_СТОРОН}`

## Специальные переменные (важно)

### `{РЕКВИЗИТЫ_СТОРОН}`

В DOCX заменяется на табличный блок:

- слева таблица `Исполнитель` (реквизиты артиста),
- справа таблица `Заказчик` (реквизиты клиента).

Пустые значения оставляются пустыми (без `_______`).

### `{ПОДПИСИ_СТОРОН}`

В DOCX заменяется на таблицу `2x2` без рамок:

- `1,1` — `Исполнитель`
- `1,2` — `Заказчик`
- `2,1` — `Фамилия И.О. исполнителя + "_______________"`
- `2,2` — `Фамилия И.О. заказчика + "_______________"`

## Имена итоговых файлов

- Договор: `Договор №<номер> от <dd.mm.yyyy>.docx`
- Акт: `Акт №<номер> от <dd.mm.yyyy>.docx`

## Где задаются реквизиты артиста

- `Документы -> Редактировать реквизиты`
- Юр. статус артиста:
  - `Индивидуальный предприниматель`
  - `Самозанятый`

Для режима `Самозанятый` поле `ОГРНИП` не обязательно.
