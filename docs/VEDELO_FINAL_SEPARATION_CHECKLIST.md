# Финальное отделение Ведело от ArtistCRM

Аудит: 2026-09-15.

## Подтверждено

- `vedelo.ru` и `www.vedelo.ru` указывают на production; TLS работает, `www`
  перенаправляется на apex.
- Google Search Console и Яндекс Вебмастер подтверждены DNS-записями.
- VK ID web включён; публичная конфигурация отдаёт app ID и callback на
  `https://vedelo.ru/api/vk-id/callback` без секрета.
- Canonical, robots и sitemap нового origin используют `vedelo.ru`.
- Код переведён на счётчик Яндекс Метрики `112668604`; сырой HTML-код счётчика
  не дублируется, Webvisor не включён, загрузка начинается после согласия.
- На новом origin убрана пользовательская подпись «ранее ArtistCRM»; она
  сохранена только в безопасном сценарии переноса старой PWA.
- Регистрация web/Android разделяет Пользовательское соглашение, ознакомление с
  Политикой и отдельное согласие на обработку персональных данных; сервер
  сохраняет дату и версии документов.

## Обязательные действия после деплоя

1. В счётчике 112668604 создать цели из `docs/SEO_POST_DEPLOY.md` и проверить
   цепочку CTA → регистрация → первая работа в чистом профиле.
2. Опубликовать новую редакцию документов и проверить `/privacy`, `/terms`,
   `/personal-data-consent`, `/payment`, `/account-deletion` на desktop/mobile.
   До включения серверного требования выпустить Android `versionCode 18`: старые
   сборки без отдельного флажка соглашения смогут входить в существующий аккаунт,
   но не смогут зарегистрировать новый.
3. Задать фактический `NEXT_PUBLIC_SUPPORT_EMAIL` только после проверки приёма
   писем. Сейчас у `vedelo.ru` нет MX.
4. Провести 30-дневную migration campaign, задав один неизменный
   `BRAND_MIGRATION_STARTED_AT`. Сейчас `artistcrm.ru` всё ещё отдаёт собственную
   главную и canonical; немедленный жёсткий redirect сломает перенос сессий и
   push установленной PWA.
5. После старта переезда применить инструменты смены адреса в свойствах старого
   домена Google Search Console и Яндекс Вебмастера, повторно отправить sitemap и
   контролировать `301/308` без циклов.
6. Проверить OAuth, платежи, webhooks, push и PWA на обоих origin по
   `docs/BRAND_AND_PWA_MIGRATION.md`.

## Почта `support@vedelo.ru`

На дату аудита корпоративная почта на своём домене входит в платные тарифы
Яндекс 360 для бизнеса; минимальный публичный тариф — 319 ₽ в месяц за
сотрудника. Бесплатно создать рабочий ящик через Яндекс 360 нельзя, поэтому
организация, MX и пользователь не создавались.

После выбора провайдера:

1. Подключить домен и подтвердить владение.
2. Создать ящик, затем добавить MX, SPF, DKIM и DMARC.
3. Проверить приём, отправку, попадание не в спам и восстановление доступа.
4. Только затем установить `NEXT_PUBLIC_SUPPORT_EMAIL`, `TOCHKA_RECEIPT_EMAIL`,
   `VAPID_SUBJECT`, SMTP и `MAIL_FROM`.

## Остаток миграции вне репозитория

- проверить обозначение «Ведело» и сходные знаки в МКТУ 9, 35 и 42 через
  официальный поиск/патентного специалиста;
- обновить уведомление Роскомнадзору об обработке персональных данных: новый
  домен, цели, категории, места баз, обработчики и применимые трансграничные
  передачи; подтвердить первичное хранение данных граждан РФ в РФ;
- проверить договоры/поручения обработки с хостингом, файловым хранилищем,
  телефонией, платежами, push и AI-провайдерами;
- перенести цели и сегменты со старого счётчика 108801563; обновить старую
  кампанию Яндекс Директа, если она будет использоваться: название бренда, URL,
  UTM и новый счётчик;
- создать Firebase Android app для `ru.escalion.vedelo`, обновить
  `google-services.json` в EAS/GitHub secrets, заполнить Android-поля VK ID,
  создать новую карточку Google Play и пройти device QA;
- выполнить реальные E2E Google OAuth, VK ID, ЮKassa, Точки, Tilda/public lead,
  VK/Avito/Telegram, web push и Android push;
- после migration campaign оставить старый домен во владении и сохранить только
  явно задокументированные технические контракты совместимости (`/api/events`,
  `eventId`, коллекции Events, storage-префиксы `artistcrm`, legacy deep link и
  заголовок service worker).
